#Requires AutoHotkey v1.1
#NoEnv
#SingleInstance Force

; The launcher only needs a valid module entrypoint. Ctrl+F4 is intentionally
; handled by engine_rollclub.ahk so there is never a competing AHK process.
ExitApp
