# Авто-КЦ (Roll Club) — повний протокол

Жива специфікація автообробки замовлень.
Файл: `RollHelper/engine_rollclub.ahk` (пульт) + `../server/` (сервер UIA/Flask).

> **КРИТИЧНО для ІІ-агентів:** Активний скрипт — `engine_rollclub.ahk`, НЕ `main.ahk`.
> `main.ahk` читає `settings.ini[App]Brand=rollclub` і запускає `engine_rollclub.ahk`.
> Будь-які правки пульта Roll Club вносити тільки в `engine_rollclub.ahk`.

---

## 1. Загальний потік (ручний режим — основний)

1. Оператор бачить замовлення в iiko → натискає **тильду (~)** → пульт читає коментар і заповнює поля.
2. Оператор перевіряє пульт (палички, оплата, адреса, час).
3. Натискає **Enter** → пробивається в iiko: коментар, кухня, адреса, час, готівка, палички.
   - Для **доставки**: ДО пробивання пульт автоматично тисне «Найти точку» в iiko і звіряє зони.
4. Натискає **Ctrl+Shift+Enter** (або свою клавішу з Налаштувань) → Подтвердить + Зберегти на точку.

> Авто-режим (F4 / 🤖) — теж є, але ручний — основний робочий режим.

---

## 2. Розбір коментаря — куди що тягнеться

| Що в коментарі | Тригер (приклад) | Куди йде |
|---|---|---|
| Оплата готівкою | `Готівкою №786130` | тип оплати = Готівка, `autoCash=1` |
| Оплата картою | `Картою на сайті №…`, `---ОПЛАЧЕНО---` | оплата = Оплачено |
| Оплата QR | `QR code №…` | оплата = QR |
| Решта | `Решта з: 1000` або `Підготувати решту з 1000` | `calcChange=1000`, `autoCash=1` |
| Решта немає | `Решта з: ні / нет / без решти` | без сдачі |
| Палички звичайні | `Звичайні: 3` | `VisNorm=3` (пробиває PLU палочок ×3) |
| Палички учбові | `Учбові: 2` / `Навчальні: 2` | `VisEdu=2` |
| Гункан | `!!!ПЕРШЕМОБ` | `autoGunkan=1` + картка «ПЕРШЕМОБ» |
| Самовивіз | `Самовивіз: Київ — Драгоманова` | `hasPickup=1`, концепція+точка |
| Коментар курʼєру | `Коментар до адреси: …` | примітка до адреси (`AddressNote`) |
| Кухня | `без X`, `не кладіть/класти/додавати`, `алерг`, `менше/більше X`, `гостро`, `наріжте`, `соус окремо`, `підписати бокс` | поле «Кухня» (`ClientInfo`) |

### Кухня vs Адреса — поділ (ВАЖЛИВО)
Коли в `Коментар до адреси:` змішані курʼєрські й кухонні фрази:
- **кухонні** («Не кладіть зелену цибулю») → у **Кухню** (`ClientInfo`);
- **курʼєрські** («Домофон не працює, зателефонуйте по приїзду») → у **Примітку адреси** (`AddressNote`).
Кухонні фрази **вирізаються** з примітки адреси, щоб не дублювати курʼєру.

---

## 3. Пробивання в iiko (ApplyRollclub)

Порядок дій при натисканні Enter (або кнопки «Внести»):

1. **Звірка точки** (тільки доставка, якщо `CHECK_POINT_ENABLED=1`):
   - Клік «Найти точку» → пауза 1800мс → {Enter} (закрити можливий попап iiko) → читання поля «Точка\*» → порівняння з нашою KML-зоною.
   - Збіг: короткий писк 1000 Гц + ToolTip `✅ Точка ОК`.
   - Розбіжність: довгий гул 400 Гц + ToolTip `⚠ ТОЧКА РІЗНИТЬСЯ! / Пульт: Приморська / iiko: Центр`.
2. Запис коментаря (`commX,commY`) — `OrderComment`.
3. Запис картки клієнта (`cardX,cardY`) — `ClientCard`.
4. Запис кухні (`infoX,infoY`) — `ClientInfo` (подвійний клік = режим редагування).
5. Запис примітки адреси (`addrX,addrY`) — `AddressNote` (тільки доставка, `!hasPickup`).
6. Запис часу (`timeX,timeY`) — `ReadyTimeH:ReadyTimeM`.
7. **Готівка/решта** (якщо `autoCash=1`): клік Готівки → вводить «Готівка» → Enter → Tab×2 → вводить `calcChange` → Enter.
8. **Подарунки** (якщо ввімкнені): пробивання PLU через NewGiftMacro.
9. **Палички** (якщо `VisNorm>0` або `VisEdu>0`): PLU → {Down} → **{Tab}** (перехід у поле кількості) → `^a{BackSpace}` → кількість → Enter.
   - ⚠️ ВАЖЛИВО: `{Tab}` а НЕ `{Enter}` після `{Down}`. `{Enter}` закриває рядок з qty=1 і `^a{BackSpace}` летить в наступний порожній PLU. `{Tab}` переходить у поле кількості того ж рядка.
10. **Самовивіз + autoMode**: Концепція → Tab → Space → PgUp → Enter.

---

## 4. Змінні оплати (КРИТИЧНО — тут був баг)

| Змінна | Що означає |
|---|---|
| `autoCash` | **ПРАВИЛЬНА** змінна готівки. Ставиться парсером (lines 988, 1002, 1005). Перемикається кнопкою. |
| `calcChange` | Сума решти (число). 0 якщо без решти. |
| `DoAutoCash` | **НЕ ІСНУЄ** як активна змінна — ніде не присвоюється. Не використовувати. |

В `ApplyRollclub` перевіряємо `autoCash == 1`, НЕ `DoAutoCash`.

---

## 5. Подарунки

### 5.1 По сумі (autoGift)
| Сума замовлення | Подарунок |
|---|---|
| ≥ 1599 грн | Крабсбургер |
| ≥ 999 грн | Сендвіч |
| ≥ 699 грн | Пепсі |

### 5.2 По купону (з панелі «Подсказки»)
Сервер читає панель «ПРЕДЛОЖИТЕ ГОСТЮ» → найдорожчий доступний: Крабсбургер > Сендвіч > Пепсі.

### 5.3 Пакет
Якщо в підказках «Потрібно додати ПАКЕТ» → пробити PLU 02901.

### PLU-коди подарунків
| Подарунок | PLU |
|---|---|
| Пепсі | 00112 |
| Суші Сендвіч три риби | 02893 |
| Крабсбургер | 02927 |
| Морс | 00111 |
| Бургер з креветкою | 02664 |
| Сендвіч з креветкою | 03234 |
| Пакет | 02901 |

---

## 6. Самовивіз — концепції (коментар → Концепція в iiko)

| `Самовивіз: …` | Концепція в iiko |
|---|---|
| Київ — Драгоманова | Київ Драгоманова |
| Харків — Конституції | РК Харків Конституції Доставка |
| Харків — Садовий проїзд | Харків Нові Дома |
| Львів — Крива Липа | Львов Липа |
| Київ — Миколи Лаврухіна | Київ Лаврухіна |
| Дніпро — Князя Ярослава Мудрого | Дніпро Мудрого |
| Київ — Січових Стрільців | Київ Стрільців |
| Київ — Антонова | Київ Антонова |
| Біла Церква — Вернадського | Біла Церква |
| Одеса — Незалежності | Приморська |
| ІФ - Української Перемоги | Франківськ Фудотека |
| Рівне — Кулика і Гудачека | Рівне |
| Вінниця — 600-річчя | Вінниця |

**Виставлення (autoMode):** клік «Концепція» → вписати назву → Tab → Space → PgUp → Enter.

---

## 7. Звірка точки (RcVerifyPoint)

Підпрограма `RcVerifyPoint` — синхронна, запускається GoSub на **початку** `ApplyRollclub` (тільки доставка, тільки якщо `CHECK_POINT_ENABLED=1` і `tochkaX != 0`).

**Алгоритм:**
1. Активує вікно iiko (`iikoWinExe`).
2. Клік по `naitiX,naitiY` («Найти точку»).
3. `Sleep 1800` — очікує геокодингу iiko.
4. `Send, {Enter}` — закриває попап «Внимание! Время доставки изменено...» (якщо є).
5. `Sleep 300`.
6. Клік по `tochkaX,tochkaY` (поле «Точка\*», текстова частина дропдауну).
7. `^c` → `ClipWait` → `iikoPoint`.
8. Порівняння: `InStr(iikoPoint, RcLastZone)` або через `Kitchens[].KmlKey`.
9. Результат: ToolTip + звук.

**Налаштування:**
- Чекбокс «Авто-Звірка Точки» в Налаштуваннях → `RkConfig.ini [Main] CheckPoint`.
- Приціл «Точка (для звірки)» → `RkConfig.ini [Targets] TochkaX/TochkaY`.
- Калібрувати: клікнути в **текстову частину** поля «Точка\*» (лівіше стрілки дропдауну).

**Відомий нюанс:** iiko іноді показує попап про зміну часу доставки. Якщо попапу НЕМАЄ — `{Enter}` може зробити щось небажане (підтвердити щось у формі). Поки що спостерігаємо, за потреби замінимо на перевірку попапу перед Enter.

**Лог:** кожна звірка пишеться в `parse_debug.log`:
```
==== POINT-CHECK 2026-06-23 22:31 | our=[Приморська] iiko=[Одеса Приморська (РК...)] match=1
```

---

## 8. Калібрування прицілів (Налаштування)

| Приціл | Що це |
|---|---|
| Коментар | поле «Комментарий к заказу» |
| Кухня | поле/колонка кухні (подвійний клік) |
| Адреса | поле «Примечание к адресу» |
| Час | поле часу готовності |
| Табл. Страв | таблиця страв (для пробивання PLU) |
| Концепція (самовивіз) | поле «Концепція» |
| Найти точку | кнопка «Найти точку» (доставка) |
| Точка (для звірки) | поле «Точка\*» — текстова частина дропдауну |
| Подтвердить (фініш) | кнопка «Подтвердить» |
| Зберегти на точку | кнопка «Сохранить на точку» |
| Поле Поиск (КЦ) | комірка фільтра колонки № у списку |
| 1-й рядок списку | рядок-результат після фільтра по № |

---

## 9. Гарячі клавіші

| Клавіша | Дія | Примітка |
|---|---|---|
| **~** (тильда) | Прочитати замовлення, заповнити пульт | завжди |
| **Enter** | Пробити в iiko (Внести) | тільки коли пульт у фокусі |
| **Ctrl+Shift+Enter** | Фініш: Подтвердить → Зберегти на точку | **змінено з Ctrl+Enter** — налаштовується |
| **F1** | Швидкий СИВ (модуль палочок) | налаштовується |
| **F4 / 🤖** | Авто-режим (монітор КЦ) увімк/вимк | |

Всі клавіші (крім Enter/тильди) налаштовуються в розділі «ГАРЯЧІ КЛАВІШІ» у Налаштуваннях → зберігаються в `RkConfig.ini [Hotkeys]`.

---

## 10. Як дебажити

Пульт **сам логує розбір** кожного читання у файл `RollHelper/parse_debug.log`.
Після кожної тильди туди пишеться блок:
```
==== 2026-06-23 09:22 | No=726136 ====
RAW    : <сирий коментар з iiko>
KITCHEN: <що піде в поле Кухня>
ADDR   : <що піде в Примітку адреси>
PAY    : Картою #985639 | change=
STICKS : norm= edu=2
```

**Процес дебагу:**
1. Заказ відпрацював криво → НЕ описуємо словами.
2. Відкрити `parse_debug.log` → скопіювати ОСТАННІЙ блок → кинути нейронці.
3. Якщо RAW неповний → проблема **читання**. Якщо RAW повний, а поля не ті → проблема **парсингу**. Якщо в логу все вірно, а в iiko інше → проблема **запису** (приціл/кліки).

При Внести також пишеться WRITE-блок:
```
==== WRITE 2026-06-23 22:30 | No=70000 hasPickup=0 ====
 comm : x=251 txt=[Готівка — решта з 1000]
 addr : x=1147 txt=[Піднятись]
 cash : do=1 crossX=1455 cashX=1472
```
Це показує, що РЕАЛЬНО передається в iiko.

---

## 11. Приклади замовлень

### Приклад 1 (доставка, Харків)
```
Пост-1 Mob ---ОПЛАЧЕНО--- Картою на сайті №985639 Коментар до адреси:
Домофон не працює, зателефонуйте по приїзду Не кладіть зелену цибулю, будь ласка
Прибори: Учбові x2 2026-06-23 18:00 Фоп Богатий П.О. Олімпійська вулиця
```
- Оплата: Картою (Оплачено), autoCash=0
- Палички: учбові ×2
- Кухня: «Не кладіть зелену цибулю»
- Адреса (курʼєр): «Домофон не працює, зателефонуйте по приїзду»
- Час: 18:00

### Приклад 2 (самовивіз, Київ)
```
Пост-1 Mob !!!ПЕРШЕМОБ Готівкою №786076
Прибори: Звичайні x2 2026-06-22 18:00 Самовивіз: Київ — Миколи Лаврухіна
```
- Гункан: так
- Оплата: Готівка, autoCash=1 (якщо є решта — вводиться)
- Палички: звичайні ×2
- Концепція: «Київ Лаврухіна»

### Приклад 3 (доставка, готівка з рештою)
```
Mob !!!ПЕРШЕМОБ Готівкою №725779 Підготувати решту з 1000
Коментар до адреси: Синие ворота рядом з kcars
Дмитрія Донського вул.
```
- Оплата: Готівка, autoCash=1
- Решта: 1000 грн (calcChange=1000)
- Адреса (курʼєр): «Синие ворота рядом з kcars»
- Гункан: так

---

## 12. Відомі баги / TODO

### ✅ ВИПРАВЛЕНО (2026-06-23)
- ✅ **Палички**: `{Enter}` → `{Tab}` після `{Down}` — пробивалась тільки 1 палочка (Enter закривав рядок з qty=1).
- ✅ **Готівка/решта**: `DoAutoCash` → `autoCash` — каса не пробивалась взагалі (DoAutoCash ніде не присвоювалась).
- ✅ **Конфлікт Знайти точку**: RcVerifyPoint тепер GoSub на початку ApplyRollclub, а не таймер — не конфліктує з кліками.
- ✅ **Гаряча клавіша фінішу**: Ctrl+Enter → Ctrl+Shift+Enter + налаштовується в UI (раніше випадково натискалась).
- ✅ **global tochkaX := 0** перезаписував значення з IniRead — звірка точки не запускалась.

### ⏳ ВІДКРИТО
- [ ] iiko попап «Внимание! Время доставки изменено» — код шле {Enter}, але якщо попапу нема — Enter може щось підтвердити в формі. Потрібно спостереження; можливо замінити на перевірку вікна попапу.
- [ ] Накопичувальні подарунки (Морс, Бургер/Сендвіч з креветкою) — підтвердити текст панелі «Подсказки».
- [ ] Наповнити приклади (п.11) новими реальними замовленнями.
- [ ] Перевірити поділ Кухня/Адреса на прикладі 1 — чи правильно парсяться KITCHEN і ADDR.

---

# ═══════════════════════════════════════
# ОНОВЛЕННЯ 2026-07-15 (сесія: дежурство, прибори, перекриття)
# ═══════════════════════════════════════

## 13. Дежурство (F4) — напівавтомат взяття заказу

**Що це:** оператор тисне **F4** → пульт сам ловить перший вільний годний заказ у списку Доставки,
відкриває його, читає, пробиває, і **зупиняється** з гучним сигналом (`LoudAlarm`). Повторний F4 — стоп.

**Ключові сутності:**
- `dutyOn` — прапор дежурства. Вмикається ТІЛЬКИ в `KcDutyToggle` (F4). Більше ніде.
- `KcDutyTick` — таймер, тикає кожні **1500мс** (було 3500), кожен тик → `GoSub KcMonitor`.
- `KcMonitor` — сам захід: активує вікно → чистить № → оновлює КЦ → питає сервер → бере.
- `KcTakeOnce` (**F6**) — один захід без циклу.
- `RhAutoToggle` (кнопка 🤖 АВТО) — стара повноавтоматика, **заглушена** `AUTO_KC_ENABLED=0` (мертва).

**Правильний порядок у KcMonitor (критично — інакше бере не той заказ):**
1. Активувати вікно iiko (Syrve Office).
2. **Стерти старий №** у полі Поиск: клік `poiskX,poiskY` → `Ctrl+A` → `{Delete}`.
3. **Оновити КЦ без кулдауна:** `Ctrl+Tab` на сусідню вкладку → пауза → `Ctrl+Tab` назад.
   (Кнопка «Обновить» / автооновлення мають кулдаун ~1 хв — тому саме Ctrl+Tab.)
4. Питати сервер: `RhGet /api/iiko/kc-list` (таймаут **12000мс**, тимчасово — поки сервер повільний).
5. Якщо `take_no` порожній/0 → тултип «нема вільних», вихід.
6. Знайдено → надрукувати № у Поиск (з `{Delete}` перед вводом), **звузити список**.
7. **ПЕРЕВІРКА перед пробиттям:** ще раз `kc-list` — список має показати РІВНО наш № (`count==1`).
   Якщо ні (ми не на Доставках / фільтр не спрацював) → **НЕ пробиваємо**, пропускаємо круг.
8. Подвійний клік по рядку → відкрити заказ.
9. **Захист «зайнято оператором»:** `WinWait, Подтверждение,, 2` (діалог «Доставка обрабатывается
   оператором … продолжить?»). Є → `{Esc}` (Нет) + пропуск. З'являється НЕ миттєво — тому WinWait до 2с.
10. Читання (`TriggerMain`) + пробиття (`ApplyRollclub`) + `SoundOk` + `kcPaused=1`.

**Стоп під час захода (`kcStop`):** повторний F4 / будь-яка ручна дія ставить `kcStop=1`;
`KcMonitor` має контрольні точки, що виходять достроково (після запиту, перед відкриттям рядка, перед пробиттям).

## 14. Захист ручної роботи — «оператор працює» (ГОЛОВНЕ)

Проблема була: живий `KcMonitor` (дежурство) перебивав ручну роботу і доігрував пошук поверх пробиття.
`SendMode Input` → послані клавіші НЕ дьоргають хоткеї, тобто це НЕ був «фантомний F4».

**Рішення (`KcStopDuty`):** будь-яка РУЧНА дія оператора — **тильда, F1, F2, F3, Enter (пробиття),
Ctrl+Enter (фініш)** — викликає `KcStopDuty`, яка:
- гасить дежурство (`dutyOn=0`, `SetTimer KcDutyTick Off`, `kcStop=1`);
- ставить вікно **`_punchUntil = A_TickCount + 12000`** («оператор працює 12с»).

У цьому вікні:
- `KcMonitor` на вході виходить (не лізе в пошук);
- `KcDutyToggle` (F4) і `KcTakeOnce` (F6) ігноруються.

`_inDutyTake=1` під час взяття дежурством — тоді `KcStopDuty` НЕ гасить (щоб дежурство не вбило само себе).
Покриває `TriggerMain` + `ApplyRollclub` всередині захода.

**TriggerMain при дежурстві:** якщо `_inDutyTake=1` — НЕ тоглить видимість пульта, а робить **повний
перечит** нового заказу (інакше тягнулись дані попереднього).

## 15. Прибори (Виделка/Ніж/Ложка) — НЕ бити палички

Раніше в авто-режимі пробивались тільки палички+соуси, вилки — ніколи. Тому клієнту з вилками летіли палички.

**Стало:** якщо в коменті є прибори (`Виделка/Ніж/Ложка`, `Виделки, ніж`, `Виделка`, `Ніж`, `Ложка` — рег. 946):
- **палички гасяться** (`parsedSticksNorm=0`, `parsedSticksEdu=0`);
- пробиваються **вилка + ніж** окремими PLU. **Ложку НЕ б'ємо.**

**Нові PLU (в конфіг, редагуються без коду):**
| Прибор | PLU | Ключ RkConfig `[PLU]` |
|---|---|---|
| Виделка одноразова | 00140 | `Fork` |
| Ніж одноразовий | 00142 | `Knife` |
| Палички звичайні | 00143 | `SticksNorm` (гасяться при приборах) |
| Прибори (набір, стара) | 02439 | `Utensils` (лишилась, не викор. у новому шляху) |

Нюанс: соуси зараз прив'язані до кількості паличок; при приборах паличок 0 → соуси теж 0.
Якщо вилочним заказам потрібні соуси — відв'язати соуси від паличок (TODO).

## 16. Перекриті вулиці (попередження) — файл, не код

**Файл:** `brands/rollclub/BlockedStreets.txt`. Пульт на кожній тильді звіряє адресу заказу зі списком.
Збіг → червона плашка «ПЕРЕКРИТО — уточни ділянку». Файл читається щоразу — **перезавантаження не треба**.

**Формат:** одна вулиця на рядок, ділянка в дужках; `#`/`;` — коментар; `# МІСТО: Київ` — обмежити містом.
Оператор просто вставляє текст оголошення (копі-паст), коли перекриття скасують — чистить рядки.

**Код:** функції `RcBlockedStreetHit(addr)` + `RcBlkNorm(s)`; глобали `blockedHit`/`blockedInfo`;
перевірка в кінці розбору; червона плашка в `DrawRollclub` під адресою.

## 17. Сервер `read_kc_list` (iiko_bridge.py) — швидкість + фільтри

- **Кеш гріда:** `_kc_panel_cache` — важкий обхід UIA-дерева (глибина 12) робиться один раз, далі рядки
  читаються з готового посилання. Якщо посилання застаріло (вкладку закрили) — перешукує.
- **Таймінг:** пише `KC-LIST timing s: cached=.. find=.. read_rows=.. TOTAL=..` у `bridge.log`.
- **Відмінені заказы:** читається колонка `Статус`; рядки `Отменён`/`Скасовано` **не беруться** (`n_cancelled`).
- **Швидкий API-шлях (не вийшло):** пробник `syrve_deliveries_probe.py` показав, що ендпоінти доставок
  класичного resto-API повертають 404 на цьому Syrve-хмарі. Для швидкого API потрібен Transport-API ключ.

## 18. Логи діагностики (parse_debug.log)

- `F4-TOGGLE (was dutyOn=..)` — кожне натискання F4.
- `F4 IGNORED — під час пробиття` — F4 заблокований вікном «оператор працює».
- `DUTY-TICK dutyOn=.. kcBusy=..` — кожен тик таймера дежурства.
- `KcMON enter dutyOn=.. kcForce=.. autoMode=.. AUTO=.. kcBusy=.. inTake=..` — стан на вході в пошук.
- (сервер, bridge.log) `KC-LIST timing …` — розбивка часу запиту заказів.

## 19. Гарячі клавіші — доповнення до п.9

| Клавіша | Дія |
|---|---|
| **F4** | Дежурство увімк/стоп (`KcDutyToggle`). Ігнорується у вікні «оператор працює». |
| **F6** | Один захід дежурства (`KcTakeOnce`). Так само ігнорується у вікні. |
| **тильда / F1 / F2 / F3 / Enter / Ctrl+Enter** | Будь-яка з них гасить дежурство і відкриває вікно «оператор працює 12с». |
