# AI_DESIGN_RULES — дизайн-система пульта Rollclub PRO (AHK v1)

> Файл-промт для ИИ-агентов и разработчиков. Перед любой правкой UI в
> `engine_rollclub.ahk` (и `main.ahk` Roll House) — прочитать целиком.
> Цель правок: **точечное улучшение читабельности**, НЕ редизайн.

---

## 0. Роль и принцип

Ты — Senior UI/UX + инженер AutoHotkey v1, работаешь с **существующим** кодом
операторской CRM доставки еды. Интерфейс — **приладова панель (dashboard)**:

- **колір = стан** (зона, оплата, время);
- **колір = дія** (кнопки СВ/ДОСТ, подарки);
- 80% серый/белый, **максимум 20% цвета** — только сигналы.
- Если интерфейс выглядит как набор цветных кнопок — **провал**.
- Если всё ещё «белое на белом» — **провал**.

**Запрещено:** менять координаты (x/y), добавлять сложные кастом-элементы,
градиенты, тени, рамки-`Border` (дают «грязь»).

---

## 1. Технические правила цвета (КРИТИЧНО — частая ошибка)

В AHK v1 два РАЗНЫХ формата цвета — их постоянно путают:

| Где | Команда | Формат | Пример |
|-----|---------|--------|--------|
| Фон контрола | `RhRegColor(hwnd, bg, tx)` | **BGR** `0xBBGGRR` | зелёный #16A34A → `0x4AA316` |
| Цвет шрифта | `Gui, Font, c…` | **RGB** `cRRGGBB` | `c1F1F1F` |
| Фон окна | `Gui, Color, …` | **RGB-строка** | `F5F6F8` |

Правило конвертации: чтобы контрол **отобразил** цвет `#RRGGBB`, в `RhRegColor`
передавай число `0xBBGGRR` (байты R и B переставлены).
Пример: красный #FDECEA → `0xEAECFD`. Если перепутать — «червоний» станет голубым.

---

## 2. Механизм покраски в этом коде

```ahk
; регистрация цвета контрола (по hwnd)
RhRegColor(hwnd, bg, tx) {
    global RhStaticColors
    RhStaticColors[hwnd] := {bg: bg, tx: tx}
}

; обработчик WM_CTLCOLORSTATIC (OnMessage 0x0138)
WM_CTLCOLORSTATIC(wParam, lParam) {
    global RhStaticColors, RhStaticBrush
    if (lParam < 0)
        lParam += 0x100000000      ; ← ОБЯЗАТЕЛЬНО: 32-бит AHK даёт hwnd со знаком
    if (!RhStaticColors.HasKey(lParam))
        return                     ; не зарегистрирован → системный дефолт (бледно)
    o := RhStaticColors[lParam]
    DllCall("gdi32\SetTextColor", "Ptr", wParam, "UInt", o.tx)
    DllCall("gdi32\SetBkColor",   "Ptr", wParam, "UInt", o.bg)
    if (!RhStaticBrush.HasKey(o.bg))
        RhStaticBrush[o.bg] := DllCall("gdi32\CreateSolidBrush", "UInt", o.bg, "Ptr")
    return RhStaticBrush[o.bg]
}
```

### Два бага, которые УЖЕ починены — НЕ ломать обратно:

1. **Знак hwnd.** В 32-битном AHK `lParam` приходит со знаком. Без строки
   `if (lParam < 0) lParam += 0x100000000` покраска срабатывает «через раз»
   (зависит от адреса окна на конкретном запуске).

2. **Первая отрисовка.** Статик-контролы — дочерние окна. На первом `Gui, Show`
   после постройки они уже нарисованы дефолтной кистью и `WM_CTLCOLORSTATIC`
   для них повторно не вызывается → цвета появляются только со второго показа.
   **Лечится принудительной перерисовкой всех детей сразу после Show:**

   ```ahk
   Gui, Roll:Show, ...
   RollHwnd := WinExist()
   ; RDW_INVALIDATE|RDW_ERASE|RDW_ALLCHILDREN|RDW_UPDATENOW = 0x185
   DllCall("RedrawWindow", "Ptr", RollHwnd, "Ptr", 0, "Ptr", 0, "UInt", 0x185)
   ```

   Любой НОВЫЙ путь, который строит и показывает GUI, обязан делать такой redraw.

### Кнопка = `Text`, а не Button

Кликабельные «кнопки» реализованы как `Gui, Add, Text, … Center +0x200 Hwnd… g<Label>`.
Красятся через `RhRegColor`. **Без `+Border`** (бриф: рамка даёт грязь) и
**без `+BackgroundTrans`** (иначе фон не отрисуется и клик «проваливается»).

---

## 3. Базовый слой (палитра в `RhApplyTheme()`)

Светлая тема (`uiTheme != "dark"`):

```ahk
RhC_BG    := "F5F6F8"   ; фон окна (мягкий серый, НЕ чистый белый)
RhC_Panel := "FFFFFF"   ; карточки/поля
RhC_Text  := "1F1F1F"   ; основные значения (RGB для Font)
RhC_Muted := "6B6B6B"   ; подписи/лейблы (RGB для Font)
```

Применение: окно → `RhC_BG`; поля ввода → `RhC_Panel`; все лейблы → `RhC_Muted`;
значения → `RhC_Text`.

### Сигнальные токены (BGR, для `RhRegColor`)

```ahk
RhB_Chip         := 0xECE9E8   ; #E8E9EC  нейтральная кнопка В ПОКОЕ (не активна)
RhB_TintGreen    := 0xE1F0D9   ; #D9F0E1  состояние OK
RhB_TintAmber    := 0xD0EFFC   ; #FCEFD0  внимание
RhB_TintRed      := 0xD5D9FA   ; #FAD9D5  критично
RhB_GiftGunkan   := 0xFBE7ED   ; #EDE7FB  фиолет
RhB_GiftPepsi    := 0xF4ECE4   ; #E4ECF4  холодный
RhB_GiftBurger   := 0xD2E7FC   ; #FCE7D2  оранж
RhB_GiftSandwich := 0xCCF1FB   ; #FBF1CC  жёлтый
RhB_Green        := 0x4AA316   ; #16A34A  сильный зелёный (найдена зона)
RhB_Red          := 0x2626DC   ; #DC2626  сильный красный (вне зоны)
```

> Новые переменные ОБЯЗАТЕЛЬНО добавлять в `global …` внутри `RhApplyTheme()`,
> иначе они станут локальными и снаружи будут пустыми.

---

## 4. Где красить (карта сигналов)

| Элемент | Состояние | Цвет (token) | Текст |
|---------|-----------|--------------|-------|
| **Зона** | найдена | `RhB_Green` | белый |
| | KML-ошибка / нет файла | `RhB_TintAmber` | тёмный |
| | вне зоны | `RhB_Red` | белый |
| **Оплата** (Готівка) | активна | `RhB_TintGreen` | тёмный |
| | не активна | `RhB_Chip` | тёмный |
| **Подарки** | выбран | свой `RhB_Gift*` | тёмный |
| | не выбран | `RhB_Chip` | тёмный |
| **Время** СВ +40 | всегда | `RhB_TintGreen` | тёмный |
| **Время** ДОСТ +90 | всегда | `RhB_TintAmber` | тёмный |

**Категории красятся ТОЛЬКО в активном состоянии.** В покое — `RhB_Chip`
(нейтральный серый: видно как кнопку, но не шумит). НЕ красить всё подряд.

---

## 5. Очистка (обязательно)

- **Никаких emoji** в GUI-лейблах (🔎🥢🍴💰💳🍳🏠🎁⚠️❓ и т.д.). Сигнал несёт ЦВЕТ,
  а не иконка. Emoji в логах/`MsgBox` можно не трогать.
- Убрать случайные цветные тексты и «синие дефолтные» элементы Windows.
- Всё через `RhApplyTheme()` — никаких хардкод-цветов вразброс.

---

## 6. Результат — критерии приёмки

После правок: интерфейс не «светится», оператор читает без напряжения,
важные состояния видны за 0.5 сек, цвет работает как подсветка, а не декор.

---

## 7. Заметки по правке файла (для агентов)

### Какой файл править

**`engine_rollclub.ahk` — единственный активный файл Roll Club.** `main.ahk` — лишь лаунчер:
читает `settings.ini[App]Brand=rollclub` и запускает `engine_rollclub.ahk`. Правки в `main.ahk`
не влияют на поведение Roll Club пульта. Всё — в `engine_rollclub.ahk`.

- GUI-label: `DrawRollclub:` (раскладка по `curY`).
- Конфиг: `brands/rollclub/RkConfig.ini` (не `RkConfig.ini` в корне RollHelper).

### Порядок инициализации — критичная ловушка

В AHK v1 авто-выполнение идёт сверху вниз. Если `IniRead, varX, ..., 182` стоит
на строке 130, а ниже на строке 250 написано `global varX := 0` — значение **обнулится**.
Правило: `global varName` без `:= value` сохраняет значение от IniRead. С `:= value` — затирает.
Пример бага: `global tochkaX := 0` затирал `IniRead tochkaX = 182` → звірка точки не работала.

### Переменные оплаты

| Переменная | Статус |
|---|---|
| `autoCash` | ✅ правильная — ставится парсером (lines 988, 1002, 1005), тоглится кнопкой |
| `calcChange` | ✅ сумма сдачи (число) |
| `DoAutoCash` | ❌ не существует как активная — нигде не присваивается, не использовать |

В `ApplyRollclub` проверять `autoCash == 1`, НЕ `DoAutoCash`.

### PLU-пробивание палочек — {Tab} не {Enter}

После `Send, {Down}` (выбор из автокомплета PLU) следующий шаг — `Send, {Tab}`.
НЕ `Send, {Enter}`. Причина: Enter закрывает строку сетки с qty=1 и фокус уходит
в следующую пустую строку PLU. Tab переходит в поле количества той же строки.
Правильная последовательность:
```ahk
SendInput, %plu%
Sleep, 300
Send, {Down}    ; выбрать из дропдауна
Sleep, 200
Send, {Tab}     ; перейти в поле кол-ва (НЕ Enter!)
Sleep, 300
Send, ^a{BackSpace}
Sleep, 50
SendInput, %qty%{Enter}
```

### RcVerifyPoint — звірка точки

Подпрограмма `RcVerifyPoint` вызывается GoSub **в начале** `ApplyRollclub` (для доставки).
НЕ через SetTimer — чтобы не конфликтовать с кликами основного флоу.
Читает поле «Точка\*» через клик + ^c (без ^a — чтобы не открывать дропдаун).
Попап iiko «Внимание! Время доставки изменено» закрывается через `Send, {Enter}` после Sleep 1800.
Переменная `RcLastZone` — зона, найденная нашим KML-геокодингом при нажатии тильды.

### Горячая клавиша финиша

`FinishOrder` привязан динамически через `Hotkey, IfWinActive, ..., %hkFinish%, FinishOrder, On`.
Читается из `RkConfig.ini [Hotkeys] Finish`, дефолт `^+Enter` (Ctrl+Shift+Enter).
Статическая привязка `^Enter::GoSub, FinishOrder` в `#IfWinActive`-блоках удалена.

### Практические правила

- Правки вносить в **реальный файл** (файловый редактор). Крупные кириллические
  файлы могут обрезаться при некоторых способах записи — после правки проверять
  целостность (последняя строка файла, отсутствие обрыва на полуслове).
- Шрифт везде: `RhFontName := "Segoe UI"`.
- После правок → трей → Reload This Script.
